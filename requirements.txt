# Runtime dependencies — pinned for reproducible production installs.
# Update minor/patch versions after regression testing.

# REST API
fastapi>=0.111.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0

# LLM API client (v1.x required — v0.x has breaking API differences)
openai>=1.0.0,<2.0.0

# Environment variable loading
python-dotenv>=1.0.0,<2.0.0

# Sentence embeddings for RAG pipeline
sentence-transformers>=2.7.0,<4.0.0

# Vector similarity search
faiss-cpu>=1.8.0,<2.0.0

# NCBI Entrez / PubMed data ingestion
biopython>=1.83,<2.0.0

# Numerical operations (FAISS + sentence-transformers dependency)
numpy>=1.26.0,<3.0.0

# SSL certificate bundle — required after removing the ssl bypass (C-1 fix)
certifi>=2024.2.2
